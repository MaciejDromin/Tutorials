spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/springtutorial?useUnicode=yes&characterEncoding=UTF-8
    username: springtutorial
    password: password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-dll: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    pl:
      mlisowski:
        springtutorial: DEBUG