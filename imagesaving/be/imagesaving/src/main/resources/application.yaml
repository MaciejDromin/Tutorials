spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/imagesaving?useUnicode=yes&characterEncoding=UTF-8
    username: tutorials
    password: tutorials
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    com:
      drominyt:
        imagesaving: DEBUG